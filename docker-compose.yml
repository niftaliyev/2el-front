services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    image: elanazfront:latest
    container_name: elanaz-frontend
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - NEXT_TELEMETRY_DISABLED=1
    restart: always
